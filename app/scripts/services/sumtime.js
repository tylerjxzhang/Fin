'use strict';

/**
 * @ngdoc service
 * @name finApp.sumTime
 * @description
 * # sumTime
 * Factory in the finApp.
 */
angular.module('finApp')
  .factory('sumTime', function(acts) {
    /*var data = [];
    console.log(acts);
    $.each(acts, function(key, value){
        var sum = 0;
        $.each(acts.time,function(){
            sum += acts.key;
        })
        data.push({
            
        });
    })
    return {
      sumOfAllIntervals: 
    };*/
  });
